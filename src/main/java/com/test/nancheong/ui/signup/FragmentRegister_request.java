/*
package com.test.nancheong.ui.signup;

import android.os.Bundle;

import androidx.fragment.app.Fragment;

import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

import com.android.volley.AuthFailureError;
import com.android.volley.Response;
import com.android.volley.toolbox.StringRequest;
import com.test.nancheong.R;

import java.net.URL;
import java.util.HashMap;
import java.util.Map;


 * A simple {@link Fragment} subclass.
 * Use the {@link FragmentRegister_request#newInstance} factory method to
 * create an instance of this fragment.

public class FragmentRegister_request<map> extends StringRequest {

    //SERVER URL(PHP 연동)
    // final static  private String URL = "";

    public FragmentRegister_request(String userID, String userPassword, String userName, int userAgE, Response.Listener<String> listener)
    //super(Method.POST, URL, listener,null);
    /* map = new HashMap<>();
    map.put(userID);
    map.put(userPassword);
    map.put(userName);
    map.put(userAge);




}
    @Override
    protected Map<String, String> getParams() throws AuthFailureError {
        //return map;
    }
}


*/

